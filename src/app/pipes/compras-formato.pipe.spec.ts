import { ComprasFormatoPipe } from './compras-formato.pipe';

describe('ComprasFormatoPipe', () => {
  it('create an instance', () => {
    const pipe = new ComprasFormatoPipe();
    expect(pipe).toBeTruthy();
  });
});
